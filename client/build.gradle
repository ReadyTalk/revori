apply plugin: 'cpp'

binaries.all {
  if (toolChain in Gcc) {
    cppCompiler.args "-Werror", "-Wall", "-Wextra", "-Wunused-parameter",
      "-Winit-self", "-fno-rtti", "-fno-exceptions", "-O0", "-g3"
    linker.args "-lreadline"
  }
}

executables {
  main {
    baseName = 'revori-client'
  }
}

